using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public enum ItemType {
    // Purpose is to easily identify what type each item is
    // not by an integer but by a readable string
   BLADE, HANDLE, TOP_HANDLE
}
